[mysqld]

# General Settings
user = mysql
port = 3306
bind-address = 0.0.0.0
datadir = /var/lib/mysql
socket = /run/mysqld/mysqld.sock

# bind to all interfaces to allow remote connections cause we are in a docker container and we want to be able to connect to mysql from other containers